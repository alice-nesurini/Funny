/* a /* a/*
 * Fibonacci sequence*/
 */ */


{fib ->
    fib = {(n) fib0, fib1, fib ->
    /* commento */
        fib0 = 1;
        fib1 = 0;
        while n > 0 do
            fib = fib0 + fib1;
            fib0 = fib1;
            fib1 = fib;
            n -= 1;
        od;
        /* non da problemi */
        /* c2 */
        fib1

    };

    println(fib(10000));
}