/* c1 */

// inline
// inline
// inline

{ x ->
    x=0;
    if x != 0 && 1/x == 1 then
        println("ok");
    else
        println(5+" ko");
        println("ko "+5);
        println("ko "+true);
        println(false+" ko");
        println("str0 "+"ko");
    fi
}